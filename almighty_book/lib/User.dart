import 'package:flutter/material.dart';

class User extends StatelessWidget {
  String firstname;
  String email;
  String dob;
  String address;
  String phone;
  String gender;

User({
    this.dob, this.firstname, this.email, this.phone, this.address, this.gender,
});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

